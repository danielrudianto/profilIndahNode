# Dependencies
node_modules

# Environment & rahasia
.env
.env.*
!.env.example
credentials

# Hasil build — dibangun ulang saat deploy, tidak perlu ikut versi
dist

# Data runtime — ini state server, bukan kode.
# data.ms = index Meilisearch, dump.rdb = snapshot Redis.
# Keduanya berisi data usaha & data user yang sebenarnya.
data.ms
dump.rdb
*.rdb

# Berkas sementara & tempelan yang sempat tercecer di akar proyek.
# Pernah ada snapshot Redis bernama "df" berisi hash sandi user asli.
*.patch
MANIFEST.txt

# Log & keluaran sementara
log.txt
src/log.csv
*.log