export enum fetchMode {
  All,
  Pagination,
  Autocomplete,
  Child,
  ChildByParentID,
  ParentAutocomplete,
  ChildAutocomplete,
  Unconfirmed,
  AllV2,
}

export enum fetchType {
  Complete,
  Simple,
}
