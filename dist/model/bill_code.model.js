"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const client_1 = require("@prisma/client");
const prisma = new client_1.PrismaClient();
class BillCodeModel {
    constructor(customer_id, created_by, payment_method_id, discount, delivery, date = null, id = null) {
        this.is_delete = false;
        this.is_confirm = true;
        // Name will be uniquely generated by our model
        if (id != null) {
            this.id = id;
        }
        this.customer_id = customer_id;
        this.created_by = created_by;
        this.created_at = new Date();
        this.payment_method_id = payment_method_id;
        this.confirmed_at = this.created_at;
        this.confirmed_by = this.created_by;
        this.discount = discount;
        this.delivery = delivery;
        this.date = date;
        this.name = this.generateName(this.created_at);
    }
    create() {
        return prisma.bill_code.create({
            data: {
                name: this.name,
                created_by: this.created_by,
                created_at: this.created_at,
                customer_id: this.customer_id,
                payment_method_id: this.payment_method_id,
                discount: this.discount,
                delivery: this.delivery,
                date: this.date,
            },
            select: {
                id: true,
                name: true,
                discount: true,
                delivery: true,
                payment_method: {
                    select: {
                        name: true,
                        description: true,
                        id: true,
                    },
                },
                customer_id: true,
                user_bill_code_created_byTouser: {
                    select: {
                        id: true,
                        name: true,
                    },
                },
            },
        });
    }
    generateName(date = new Date()) {
        let name = "";
        this.countByDate(new Date(date.setHours(0, 0, 0, 0)))
            .then((count) => {
            // Bill code count on the same date
            // Pad start 8 digits "0" to the count added by 1
            name = `${date.getFullYear()}${(date.getMonth() + 1)
                .toString()
                .padStart(2, "0")}${date.getDate().toString().padStart(2, "0")}-${(count + 1)
                .toString()
                .padStart(8, "0")}`;
        })
            .catch((error) => {
            throw Error(error);
        });
        return name;
    }
    static countItemByReference(reference) {
        return prisma.bill.count({
            where: {
                item: {
                    reference: reference,
                },
            },
        });
    }
    static countByCustomerId(customer_id) {
        return prisma.bill_code.count({
            where: {
                customer_id: customer_id,
                is_delete: false,
            },
        });
    }
    static countByCustomerIds(customer_ids) {
        return prisma.bill_code.groupBy({
            by: ["customer_id"],
            where: {
                customer_id: {
                    in: customer_ids,
                },
                is_delete: false,
            },
            _count: true,
        });
    }
    countByDate(date) {
        // Time is already adjusted to 00:00:00.00
        const start_date = date;
        const end_date = date;
        end_date.setDate(end_date.getDate() + 1);
        return prisma.bill_code.count({
            where: {
                AND: [
                    {
                        created_at: {
                            lt: end_date,
                        },
                    },
                    {
                        created_at: {
                            gte: start_date,
                        },
                    },
                ],
            },
        });
    }
    static countByPaymentMethodIds(payment_method_ids) {
        return prisma.bill_code.groupBy({
            by: ["payment_method_id"],
            where: {
                payment_method_id: {
                    in: payment_method_ids,
                },
                is_delete: false,
            },
            _count: true,
        });
    }
    static countByPaymentMethodId(payment_method_id) {
        return prisma.bill_code.count({
            where: {
                payment_method_id: payment_method_id,
                is_delete: false,
            },
        });
    }
}
exports.default = BillCodeModel;
